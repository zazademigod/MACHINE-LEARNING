#an arithmetic operation to calculate the total seconds in 4 years 


years = 4

months_per = 12

days_per = 30

hours_per = 24

minutes_per = 60

secs_per = 60

total_secs = years*months_per*days_per*hours_per*minutes_per*secs_per

print(total_secs)


